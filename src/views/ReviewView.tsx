import type { Period } from "../api";
import { hours } from "../api";
import { messages as m } from "../constants/messages";
import { Button, Card } from "../components/ui";

export const ReviewView = ({ period, onSelect }: { period: Period; onSelect: (date: string) => void }) => <div className="grid gap-5"><div className="flex items-end justify-between gap-4"><div><p className="mb-0 text-[11px] font-bold uppercase tracking-[.08em] text-[#6684ad]">{m.periodReview}</p><h2 className="mt-1 text-[23px] font-bold tracking-[-.03em] text-ink">{m.review}</h2></div></div><Card><div className="overflow-x-auto"><table className="w-full min-w-[700px] border-collapse text-xs"><thead><tr>{[m.date, m.dayType, m.regularHours, m.overtimeHours, m.nightOtHours, m.restDayHours, ""].map((heading) => <th key={heading} className="border-b border-slate-100 px-2.5 py-3 text-left text-[11px] uppercase tracking-[.04em] text-slate-500">{heading}</th>)}</tr></thead><tbody>{period.dailyRecords.length === 0 ? <tr><td className="px-2.5 py-3" colSpan={7}>{m.noRecords}</td></tr> : period.dailyRecords.map((record) => <tr key={record.id} className="border-b border-slate-100"><td className="whitespace-nowrap px-2.5 py-3">{record.workDate}</td><td className="whitespace-nowrap px-2.5 py-3">{record.dayType}</td><td className="whitespace-nowrap px-2.5 py-3">{hours(record.regularMinutes)}</td><td className="whitespace-nowrap px-2.5 py-3">{hours(record.regularOtMinutes)}</td><td className="whitespace-nowrap px-2.5 py-3">{hours(record.nightDiffOtMinutes)}</td><td className="whitespace-nowrap px-2.5 py-3">{hours(record.restDayMinutes + record.restDayOtMinutes)}</td><td className="px-2.5 py-3"><Button className="px-2 py-1.5 text-[11px]" secondary onClick={() => onSelect(record.workDate)}>{m.dailyEntry}</Button></td></tr>)}</tbody></table></div></Card></div>;
